{
  "name": "zknox-blue",
  "version": "1.0.0",
  "description": "ZKNOX Blue - Official Ledger app installer (WebHID)",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.js --bundle --format=esm --outfile=dist/ledgerjs.bundle.js --platform=browser",
    "serve": "python3 -m http.server 8080"
  },
  "dependencies": {
    "@noble/hashes": "1.8.0",
    "@noble/secp256k1": "2.3.0"
  },
  "devDependencies": {
    "esbuild": "0.27.7"
  }
}
